<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Spendify"
        tools:targetApi="31">

        <!-- ACTIVITY PANTALLA INICIAR SESION-->
        <activity
            android:name=".PantallaIniciarSesion"
            android:exported="false" />

        <!-- ACTIVITY PRIMERA PANTALLA-->
        <activity
            android:name=".PrimeraPantallaActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- ACTIVITY PANTALLA CREAR UNA CUENTA-->
        <activity
            android:name=".PantallaCrearCuenta"
            android:exported="true">
        </activity>

    </application>

</manifest>